<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AutoAuction_H2.ViewModels"
             x:Class="AutoAuction_H2.Views.ContentPanels.TrucksView"
             x:DataType="vm:TrucksViewModel">

	<DockPanel Margin="10">

		<!-- Overskrift -->
		<TextBlock Text="🚛 Lastbiler"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"
                   DockPanel.Dock="Top"/>

		<!-- Datagrid med lastbiler -->
		<DataGrid ItemsSource="{Binding Trucks}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
				            ContextRequested="DataGrid_OnContextRequested"
          DoubleTapped="DataGrid_OnDoubleTapped">

			<DataGrid.Columns>
				<DataGridTextColumn Header="Køretøj" Binding="{Binding Vehicle.Name}" />
				<DataGridTextColumn Header="Sælger" Binding="{Binding Seller.UserName}" />
				<DataGridTextColumn Header="Mindstepris" Binding="{Binding MinPrice}" />
				<DataGridTextColumn Header="Nuværende bud" Binding="{Binding CurrentBid}" />
				<DataGridTextColumn Header="Slutter" Binding="{Binding EndTime}" />
			</DataGrid.Columns>
		</DataGrid>
	</DockPanel>
</UserControl>
